#include "Quadtree.h"

//function that sets animals
std::vector<Point> setAnimals(int, int, int, int, int, Quadtree*);

//function that checks if crocodiles are crossing
bool areCrossing(Point, Point, Point, Point);
void test1(int);
void test2(int);
float crossProduct(Point, Point);
bool onRectangle(Point, Point, Point);

